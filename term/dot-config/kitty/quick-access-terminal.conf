# Lazygit quick-access terminal.

lines 40
hide_on_focus_loss yes
background_opacity 1.0
app_id kitty-lazygit

# Hide the bottom tab/status bar so the lazygit TUI fills the panel.
kitty_override tab_bar_style=hidden
