#!/usr/bin/env bash

runtime_root=${XDG_RUNTIME_DIR:-"$HOME/.cache/nvim"}
kitty_instance_id=$(uuidgen)
KITTY_OS_INSTANCE_ID="$kitty_instance_id" \
    NVIM_KITTY_LISTEN_ADDRESS="${runtime_root}/nvim-kitty-${kitty_instance_id}.sock" \
    ~/.local/kitty.app/bin/kitty "$@"
