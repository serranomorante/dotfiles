diff --git a/scripts/keyd-application-mapper b/scripts/keyd-application-mapper
index 91a365b..ccec978 100755
--- a/scripts/keyd-application-mapper
+++ b/scripts/keyd-application-mapper
@@ -121,6 +121,14 @@ class KDE():
         self.on_window_change = on_window_change
         dbus.mainloop.glib.DBusGMainLoop(set_as_default=True)
 
+        # Only use the KDE monitor if KWin's D-Bus service is present.
+        # This allows Plasma users running a different window manager to fall back
+        # to other monitors (e.g. XMonitor or Wlroots) instead of failing later.
+        bus = dbus.SessionBus()
+        if not bus.name_has_owner('org.kde.KWin'):
+            raise Exception('org.kde.KWin D-Bus service not available')
+
+
     # Inject the kwin script
     def init(self):
         import dbus
