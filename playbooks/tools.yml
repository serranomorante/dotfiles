---
- hosts: localhost
  vars:
    user_os: "{{ ansible_distribution | lower | replace(' ', '') }}" # see: https://github.com/ansible/ansible/issues/30600
    home_path: /home/serranomorante # reliable... neither tilde (~), nor ($HOME) work when running as sudo
  roles:
    - system-tools # general system packages
    - lang-tools # lsp, dap, syntax, linters, formatters
    - dev-tools # editors
    # - music-tools # DAWs, VSTs, pipewire, etc
    # - browsing-tools # browsers, etc
    # - recording-tools # obs, etc
    # - monitoring-tools # cpu monitoring, etc
    # - writing-tools # notes, etc

