---
- block:
    - name: Setup note-taking utility packages
      ansible.builtin.include_tasks: "{{ task_file }}"
      with_first_found:
        - files: 10-setup-note-taking-utilities.{{ user_os }}.yml
          skip: true
      when: "user_os in ['archlinux']"
      loop_control:
        loop_var: task_file
  tags: ["40-10"]

- block:
    - name: Setup HPI setup
      ansible.builtin.include_tasks: "{{ task_file }}"
      with_first_found:
        - files: 20-setup-HPI.{{ user_os }}.yml
          skip: true
      when: "user_os in ['archlinux']"
      loop_control:
        loop_var: task_file
  tags: ["40-20"]

- block:
    - name: Setup via self-hosting
      ansible.builtin.include_tasks: "{{ task_file }}"
      with_first_found:
        - files: 25-setup-via.{{ user_os }}.yml
          skip: true
      when: "user_os in ['archlinux']"
      loop_control:
        loop_var: task_file
  tags: ["40-25"]

- block:
    - name: Setup PKM exports
      ansible.builtin.include_tasks: "{{ task_file }}"
      with_first_found:
        - files: 30-setup-exports.{{ user_os }}.yml
          skip: true
      when: "user_os in ['archlinux']"
      loop_control:
        loop_var: task_file
  tags: ["40-30"]

- block:
    - name: Setup reading tools
      ansible.builtin.include_tasks: "{{ task_file }}"
      with_first_found:
        - files: 40-setup-reading-tools.{{ user_os }}.yml
          skip: true
      when: "user_os in ['archlinux']"
      loop_control:
        loop_var: task_file
  tags: ["40-40"]

- block:
    - name: Setup calibre-web annotation
      ansible.builtin.include_tasks: "{{ task_file }}"
      with_first_found:
        - files: 45-setup-calibre-annotation.{{ user_os }}.yml
          skip: true
      when: "user_os in ['archlinux']"
      loop_control:
        loop_var: task_file
  tags: ["40-45"]

- block:
    - name: Setup LAN DNS
      ansible.builtin.include_tasks: "{{ task_file }}"
      with_first_found:
        - files: 46-setup-lan-dns.{{ user_os }}.yml
          skip: true
      when: "user_os in ['archlinux']"
      loop_control:
        loop_var: task_file
  tags: ["40-46"]

- block:
    - name: Setup notes agent context links
      ansible.builtin.include_tasks: 50-setup-notes-agent-context.yml
  tags: ["40-50"]
