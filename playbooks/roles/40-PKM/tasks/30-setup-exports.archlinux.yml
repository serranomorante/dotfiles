---
- name: "Setup exports: ensure export & index timers"
  ansible.builtin.systemd_service:
    scope: user
    name: "{{ item }}.timer"
    state: started
    enabled: true
    masked: false
  loop:
    - hypexport
    - browserexport
    - promnesia-index
