---
- name: "Lua: ensure LSP"
  become: true
  ansible.builtin.package:
    state: present
    name: lua-language-server

- name: "Lua: ensure FORMATTERS"
  become: true
  ansible.builtin.package:
    state: present
    name: stylua
