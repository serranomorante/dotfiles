# {{ ansible_managed }}
quiet
# Reaper MCP server profile. It is launched through fj-py and communicates over
# stdio; project files are exposed explicitly by the wrapper/template.
include fj-py.profile
