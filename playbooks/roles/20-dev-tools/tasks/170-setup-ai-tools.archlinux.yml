---
- name: "[archlinux] Setup ai tools: install packages"
  become: true
  ansible.builtin.package:
    state: present
    name: openai-codex

- name: "[archlinux] Setup ai tools: ensure npm prefix"
  ansible.builtin.file:
    path: "{{ node_ai_npm_prefix }}"
    state: directory
    mode: "755"

- name: "[archlinux] Setup ai tools: ensure npm packages"
  community.general.npm:
    name: "{{ item.name }}"
    version: "{{ item.version }}"
    global: true
    state: present
    executable: "{{ ansible_firejail_npm_executable }}"
  environment:
    ANSIBLE_FIREJAIL_NPM_NODE_VERSION: "{{ node_system_default_version }}"
    ANSIBLE_FIREJAIL_NPM_PREFIX: "{{ node_ai_npm_prefix }}"
  loop: "{{ node_ai_npm_packages }}"

- name: "[archlinux] Setup ai tools: symlink npm package binaries"
  ansible.builtin.file:
    state: link
    src: "{{ node_ai_npm_prefix }}/bin/{{ item.1 }}"
    dest: "{{ node_user_bin_dir }}/{{ item.1 }}"
  loop: "{{ node_ai_npm_packages | subelements('bins') }}"

- name: "[archlinux] Setup ai tools: remove postman mcp from global npm prefix"
  community.general.npm:
    name: "@postman/postman-mcp-server"
    global: true
    state: absent
    executable: "{{ ansible_firejail_npm_executable }}"
  environment:
    ANSIBLE_FIREJAIL_NPM_NODE_VERSION: "{{ node_system_default_version }}"
    ANSIBLE_FIREJAIL_NPM_PREFIX: "{{ node_ai_npm_prefix }}"

- name: "[archlinux] Setup ai tools: remove postman mcp global symlink"
  ansible.builtin.file:
    path: "{{ node_user_bin_dir }}/postman-mcp-server"
    state: absent

- name: "[archlinux] Setup ai tools: ensure postman mcp project path"
  ansible.builtin.file:
    path: "{{ postman_mcp_commentary_server_setup.project_path }}"
    state: directory
    mode: "755"

- name: "[archlinux] Setup ai tools: stat postman pnpm modules metadata"
  ansible.builtin.stat:
    path: "{{ postman_mcp_commentary_server_setup.project_path }}/node_modules/.modules.yaml"
  register: var_stat_postman_mcp_modules_yaml

- name: "[archlinux] Setup ai tools: read postman pnpm modules metadata"
  ansible.builtin.slurp:
    src: "{{ postman_mcp_commentary_server_setup.project_path }}/node_modules/.modules.yaml"
  register: var_slurp_postman_mcp_modules_yaml
  when: var_stat_postman_mcp_modules_yaml.stat.exists

- name: "[archlinux] Setup ai tools: extract postman pnpm store path"
  ansible.builtin.set_fact:
    postman_mcp_node_modules_store_dir: >-
      {{
        (
          var_slurp_postman_mcp_modules_yaml.content
          | b64decode
          | from_yaml
        ).storeDir | default('')
      }}
  when: var_stat_postman_mcp_modules_yaml.stat.exists

- name: "[archlinux] Setup ai tools: remove legacy postman node_modules"
  ansible.builtin.file:
    path: "{{ postman_mcp_commentary_server_setup.project_path }}/node_modules"
    state: absent
  when:
    - var_stat_postman_mcp_modules_yaml.stat.exists
    - postman_mcp_node_modules_store_dir | length > 0
    - postman_mcp_node_modules_store_dir != postman_mcp_commentary_server_setup.host_store_dir
    - not postman_mcp_node_modules_store_dir.startswith(postman_mcp_commentary_server_setup.host_store_dir ~ '/')

- name: "[archlinux] Setup ai tools: ensure postman mcp server"
  community.general.pnpm:
    name: "@postman/postman-mcp-server"
    version: "{{ postman_mcp_commentary_server_setup.version }}"
    path: "{{ postman_mcp_commentary_server_setup.project_path }}"
    production: true
    state: present
    executable: "{{ ansible_firejail_pnpm_executable }}"
  environment:
    ANSIBLE_FIREJAIL_PNPM_NODE_VERSION: "{{ node_system_default_version }}"

- name: "[archlinux] Setup ai tools: execute playwright install"
  ansible.builtin.command:
    argv:
      - "{{ node_system_default_node_path }}"
      - "{{ node_ai_npm_prefix }}/bin/playwright"
      - install
  environment:
    PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD: 1
  args:
    creates: ~/.cache/ms-playwright/chromium-*/INSTALLATION_COMPLETE

- name: "[archlinux] Setup ai tools: clone reaper mcp"
  ansible.builtin.git:
    repo: https://github.com/dschuler36/reaper-mcp-server
    dest: "{{ reaper_mcp_server_setup.repo_path }}"
    version: main

- name: "[archlinux] Setup ai tools: ensure reaper-mcp-server"
  ansible.builtin.command:
    argv:
      - "{{ python_firejail_executable }}"
      - online
      - "{{ reaper_mcp_server_setup.repo_path }}"
      - --
      - uv
      - venv
      - --seed
      - .venv
  args:
    creates: "{{ reaper_mcp_server_setup.repo_path }}/.venv/bin/activate"
    chdir: "{{ reaper_mcp_server_setup.repo_path }}"

- name: "[archlinux] Setup ai tools: install reaper-mcp-server"
  ansible.builtin.command:
    argv:
      - "{{ python_firejail_executable }}"
      - online
      - "{{ reaper_mcp_server_setup.repo_path }}"
      - --
      - "{{ reaper_mcp_server_setup.repo_path }}/.venv/bin/python"
      - -m
      - pip
      - install
      - .
      - uv
  args:
    chdir: "{{ reaper_mcp_server_setup.repo_path }}"
  changed_when: true

- name: "[archlinux] Setup ai tools: register figma api key"
  ansible.builtin.command: kwallet-query --folder {{ folder }} --read-password {{ passkey }} {{ wallet }}
  changed_when: true
  register: mcp_server_figma_key
  ignore_errors: true
  vars:
    folder: "{{ figma_mcp_server_setup.keyrings.folder }}"
    passkey: "{{ figma_mcp_server_setup.keyrings.passkey }}"
    wallet: "{{ figma_mcp_server_setup.keyrings.wallet }}"

- name: "[archlinux] Setup ai tools: register postman api key"
  ansible.builtin.command: kwallet-query --folder {{ folder }} --read-password {{ passkey }} {{ wallet }}
  changed_when: true
  register: mcp_server_postman_key
  ignore_errors: true
  vars:
    folder: "{{ postman_mcp_commentary_server_setup.keyrings.folder }}"
    passkey: "{{ postman_mcp_commentary_server_setup.keyrings.passkey }}"
    wallet: "{{ postman_mcp_commentary_server_setup.keyrings.wallet }}"

- name: "[archlinux] Setup ai tools: register playwright_mcp_browser_key"
  ansible.builtin.command: kwallet-query --folder {{ folder }} --read-password {{ passkey }} {{ wallet }}
  changed_when: true
  register: var_playwright_mcp_browser_key
  ignore_errors: true
  vars:
    folder: "{{ playwright_mcp_server_setup.keyrings.folder }}"
    passkey: "{{ playwright_mcp_server_setup.keyrings.passkey }}"
    wallet: "{{ playwright_mcp_server_setup.keyrings.wallet }}"

- name: "[archlinux] Setup ai tools: create dest folder"
  ansible.builtin.file:
    path: ~/.config/mcphub
    state: directory
    mode: "744"

- name: "[archlinux] Setup ai tools: generate servers.json"
  ansible.builtin.template:
    src: mcphub_servers.json
    dest: ~/.config/mcphub/servers.json
    mode: "644"
  register: var_mcp_hub_servers_json

- name: "[archlinux] Setup ai tools: restart server list"
  ansible.builtin.command: ~/bin/nvim --headless -c "lua require("mcphub").get_hub_instance():restart()" -c "quit"
  changed_when: true
  when: var_mcp_hub_servers_json.changed
  ignore_errors: true

- name: "[archlinux] Setup ai tools: stat gemini settings"
  ansible.builtin.stat:
    path: ~/.gemini/settings.json
  register: var_stat_gemini_settings

- name: "[archlinux] Setup ai tools: get gemini settings file"
  ansible.builtin.set_fact:
    gemini_settings_json: "{{ lookup('file', '~/.gemini/settings.json') | default({}) | from_json }}"
  when: var_stat_gemini_settings.stat.exists

- name: "[archlinux] Setup ai tools: combine gemini settings with mcphub servers"
  ansible.builtin.set_fact:
    gemini_settings_json: "{{ gemini_settings_json | combine(lookup('file', '~/.config/mcphub/servers.json') | default({}) | from_json) }}"
  when:
    - var_stat_gemini_settings.stat.exists
    - gemini_settings_json.values() | length != 0

- name: "[archlinux] Setup ai tools: override gemini settings file"
  ansible.builtin.copy:
    dest: ~/.gemini/settings.json
    content: "{{ gemini_settings_json }}"
    mode: "644"
  when:
    - var_stat_gemini_settings.stat.exists
    - gemini_settings_json.values() | length != 0

- name: "[archlinux] Setup ai tools: stat claude settings"
  ansible.builtin.stat:
    path: ~/.claude.json
  register: var_stat_claude_settings

- name: "[archlinux] Setup ai tools: get claude settings file"
  ansible.builtin.set_fact:
    claude_settings_json: "{{ lookup('file', '~/.claude.json') | default({}) | from_json }}"
  when: var_stat_claude_settings.stat.exists

- name: "[archlinux] Setup ai tools: combine claude settings with mcphub servers"
  ansible.builtin.set_fact:
    claude_settings_json: "{{ claude_settings_json | combine(lookup('file', '~/.config/mcphub/servers.json') | default({}) | from_json) }}"
  when:
    - var_stat_claude_settings.stat.exists
    - claude_settings_json.values() | length != 0

- name: "[archlinux] Setup ai tools: override claude settings file"
  ansible.builtin.copy:
    dest: ~/.claude.json
    content: "{{ claude_settings_json }}"
    mode: "644"
  when:
    - var_stat_claude_settings.stat.exists
    - claude_settings_json.values() | length != 0

- name: "[archlinux] Setup ai tools: create codex config folder"
  ansible.builtin.file:
    path: ~/.codex
    state: directory
    mode: "744"

- name: "[archlinux] Setup ai tools: load mcphub servers.json"
  ansible.builtin.set_fact:
    mcphub_servers_json: "{{ lookup('file', '~/.config/mcphub/servers.json') | from_json }}"

- name: "[archlinux] Setup ai tools: write codex mcp_servers into config.toml"
  ansible.builtin.blockinfile:
    path: ~/.codex/config.toml
    create: true
    mode: "644"
    marker: "# {mark} ANSIBLE MANAGED MCP SERVERS (mcphub -> codex)"
    block: "{{ lookup('template', 'codex_mcp_servers.toml.j2') }}"
