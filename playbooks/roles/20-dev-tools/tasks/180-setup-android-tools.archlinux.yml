---
- name: "[{{ user_os }}] Setup android tools: install packages"
  become: true
  ansible.builtin.package:
    state: present
    name: android-tools

- name: "[{{ user_os }}] Setup android tools: ensure folder"
  ansible.builtin.file:
    path: "{{ item }}"
    state: directory
    mode: "770"
  loop:
    - "{{ ansible_facts.env.HOME }}/data/apps/android-tools"
    - "{{ ansible_facts.env.HOME }}/data/apps/android-tools/downloads"

- name: "[{{ user_os }}] Setup android tools: stat gnirehtet marker"
  ansible.builtin.include_role:
    name: dotfiles-markers
    tasks_from: check
  vars:
    dotfiles_marker_name: "gnirehtet-{{ arch_gnirehtet_version }}"
    dotfiles_marker_register: var_gnirehtet_marker

- name: "[{{ user_os }}] Setup android tools: download gnirehtet rust version"
  # Make sure to enable developer mode and usb debugger in your device
  ansible.builtin.command:
    argv:
      - "{{ python_firejail_executable }}"
      - online
      - "{{ ansible_facts.env.HOME }}/data/apps/android-tools/downloads"
      - --
      - curl
      - -L
      - -o
      - "{{ ansible_facts.env.HOME }}/data/apps/android-tools/downloads/gnirehtet-rust-linux64-v{{ arch_gnirehtet_version }}.zip"
      - "https://github.com/Genymobile/gnirehtet/releases/download/v{{ arch_gnirehtet_version }}/gnirehtet-rust-linux64-v{{ arch_gnirehtet_version }}.zip"
  environment:
    FJ_PY_WRITABLE_PATHS: |
      {{ ansible_facts.env.HOME }}/data/apps/android-tools/downloads
  args:
    creates: "{{ ansible_facts.env.HOME }}/data/apps/android-tools/downloads/gnirehtet-rust-linux64-v{{ arch_gnirehtet_version }}.zip"
  when: not var_gnirehtet_marker.stat.exists

- name: "[{{ user_os }}] Setup android tools: install gnirehtet rust version"
  ansible.builtin.command:
    argv:
      - "{{ python_firejail_executable }}"
      - offline
      - "{{ ansible_facts.env.HOME }}/data/apps/android-tools/downloads"
      - --
      - bsdtar
      - -xf
      - "{{ ansible_facts.env.HOME }}/data/apps/android-tools/downloads/gnirehtet-rust-linux64-v{{ arch_gnirehtet_version }}.zip"
      - -C
      - "{{ ansible_facts.env.HOME }}/data/apps/android-tools"
  environment:
    FJ_PY_WRITABLE_PATHS: |
      {{ ansible_facts.env.HOME }}/data/apps/android-tools
      {{ ansible_facts.env.HOME }}/data/apps/android-tools/downloads
  when: not var_gnirehtet_marker.stat.exists
  changed_when: true
  notify: handler_ensure_gnirehtet_service

- name: "[{{ user_os }}] Setup android tools: record gnirehtet marker"
  ansible.builtin.include_role:
    name: dotfiles-markers
    tasks_from: record
  vars:
    dotfiles_marker_name: "gnirehtet-{{ arch_gnirehtet_version }}"
    dotfiles_marker_content: "{{ arch_gnirehtet_version }}\n"
    dotfiles_marker_notify:
      - handler_ensure_gnirehtet_service
  when: not var_gnirehtet_marker.stat.exists
