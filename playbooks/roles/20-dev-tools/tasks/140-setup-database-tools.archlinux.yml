---
- name: "[archlinux] Setup database tools: ensure packages"
  become: true
  ansible.builtin.package:
    state: present
    name: dbeaver

- name: "[archlinux] Setup database tools: ensure aur packages"
  aur:
    use: yay
    state: present
    name:
      - datagrip-jre
      - datagrip
