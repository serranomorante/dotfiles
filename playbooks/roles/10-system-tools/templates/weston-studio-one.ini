[shell]
# {{ ansible_managed }}
# Nested weston hosting Studio One 7: drop the default top panel so it cannot
# overlay the app's menu bar, and use a neutral dark backdrop.
panel-position=none
background-color=0xff171717
