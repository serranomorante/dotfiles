---
- name: "[archlinux] Nvidia tools: ensure nvidia packages"
  become: true
  ansible.builtin.package:
    name:
      - nvidia-lts
      - nvidia-prime
      - nvidia-settings
      - nvidia-utils
      - lib32-nvidia-utils
  when: is_x_display_session
