---
- name: "Dotfiles: ensure stow wrapper directory"
  ansible.builtin.file:
    path: "{{ dotfiles_stow_wrapper_path | dirname }}"
    state: directory
    mode: "755"

- name: "Dotfiles: install AI agent Stow target profile generator"
  ansible.builtin.template:
    src: dotfiles-ai-agent-stow-targets
    dest: "{{ dotfiles_stow_wrapper_path | dirname }}/dotfiles-ai-agent-stow-targets"
    mode: "755"

- name: "Dotfiles: install stow wrapper"
  ansible.builtin.template:
    src: dotfiles-stow
    dest: "{{ dotfiles_stow_wrapper_path }}"
    mode: "755"
