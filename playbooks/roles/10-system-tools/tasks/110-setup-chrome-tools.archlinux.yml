---
- name: "[archlinux] Chrome tools: ensure browser as debugger service"
  ansible.builtin.systemd_service:
    scope: user
    name: browser-debugger.service
    state: started
    enabled: true
    masked: false
