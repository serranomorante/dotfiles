diff --git a/dwm.c b/dwm.c
index 5c4f304..dab78c6 100644
--- a/dwm.c
+++ b/dwm.c
@@ -99,6 +99,7 @@ struct Client {
 	int isfixed, isfloating, isurgent, neverfocus, oldstate, isfullscreen, cantfocus;
 	int floatborderpx;
 	int hasfloatbw;
+	int hasfloatx, hasfloaty, hasfloatw, hasfloath;
 	Client *next;
 	Client *snext;
 	Monitor *mon;
@@ -323,10 +324,22 @@ applyrules(Client *c)
 				c->hasfloatbw = 1;
 			}
 			if (r->isfloating) {
-				if (r->floatx >= 0) c->x = c->mon->mx + r->floatx;
-				if (r->floaty >= 0) c->y = c->mon->my + r->floaty;
-				if (r->floatw >= 0) c->w = r->floatw;
-				if (r->floath >= 0) c->h = r->floath;
+				if (r->floatx >= 0) {
+					c->x = c->mon->mx + r->floatx;
+					c->hasfloatx = 1;
+				}
+				if (r->floaty >= 0) {
+					c->y = c->mon->my + r->floaty;
+					c->hasfloaty = 1;
+				}
+				if (r->floatw >= 0) {
+					c->w = r->floatw;
+					c->hasfloatw = 1;
+				}
+				if (r->floath >= 0) {
+					c->h = r->floath;
+					c->hasfloath = 1;
+				}
 			}
 			for (m = mons; m && m->num != r->monitor; m = m->next);
 			if (m)
@@ -356,7 +369,7 @@ applyrules(Client *c)
 int
 applysizehints(Client *c, int *x, int *y, int *w, int *h, int interact)
 {
-	int baseismin;
+	int baseismin, rulew = *w, ruleh = *h;
 	Monitor *m = c->mon;
 
 	/* set minimum possible */
@@ -413,6 +426,10 @@ applysizehints(Client *c, int *x, int *y, int *w, int *h, int interact)
 			*w = MIN(*w, c->maxw);
 		if (c->maxh)
 			*h = MIN(*h, c->maxh);
+		if (c->hasfloatw)
+			*w = rulew;
+		if (c->hasfloath)
+			*h = ruleh;
 	}
 	return *x != c->x || *y != c->y || *w != c->w || *h != c->h;
 }
@@ -625,20 +638,28 @@ configurerequest(XEvent *e)
 		else if (c->isfloating || !selmon->lt[selmon->sellt]->arrange) {
 			m = c->mon;
 			if (ev->value_mask & CWX) {
-				c->oldx = c->x;
-				c->x = m->mx + ev->x;
+				if (!c->hasfloatx) {
+					c->oldx = c->x;
+					c->x = m->mx + ev->x;
+				}
 			}
 			if (ev->value_mask & CWY) {
-				c->oldy = c->y;
-				c->y = m->my + ev->y;
+				if (!c->hasfloaty) {
+					c->oldy = c->y;
+					c->y = m->my + ev->y;
+				}
 			}
 			if (ev->value_mask & CWWidth) {
-				c->oldw = c->w;
-				c->w = ev->width;
+				if (!c->hasfloatw) {
+					c->oldw = c->w;
+					c->w = ev->width;
+				}
 			}
 			if (ev->value_mask & CWHeight) {
-				c->oldh = c->h;
-				c->h = ev->height;
+				if (!c->hasfloath) {
+					c->oldh = c->h;
+					c->h = ev->height;
+				}
 			}
 			if ((c->x + c->w) > m->mx + m->mw && c->isfloating)
 				c->x = m->mx + (m->mw / 2 - WIDTH(c) / 2); /* center in x direction */
