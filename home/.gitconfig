[user]
	email = serranomorantepatricio@gmail.com
	name = Patricio Serrano

[init]
	defaultBranch = main

[rerere]
	enabled = true

[core]
	editor = "tmux select-window -t {start}" \
		&& "tmux if-shell -F '#{window_zoomed_flag}' 'resize-pane -Z' ''" \
		&& "tmux select-pane -t {top-left} && nvr --nostart --remote-tab-wait-silent"
