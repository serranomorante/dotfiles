# CHANNEL CC EDGE COMMAND...
# Feedback-only mapper for the piano instrument profile: no host actions.
# This instance exists so the TFT receives live pedalboard state from the
# continuous damper (CC64) and switches while the Arduino is in piano mode.
