# Work audio is recorded from capture.sink_node.work.monitor on demand.
# Do not create a static combine-source here; it keeps its inputs running while
# applications play through sink_node.work.
context.modules = []
