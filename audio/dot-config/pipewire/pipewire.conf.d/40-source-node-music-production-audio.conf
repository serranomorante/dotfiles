context.modules = [
  {
    name = libpipewire-module-combine-stream
    args = {
      combine.mode = source
      node.name = "source_node.music-production-audio"
      node.description = "[source node] music production audio"
      combine.props = {
        audio.position = [ FL FR ]
      }
      stream.props = {
        stream.dont-remix = true
      }
      stream.rules = [
        {
          matches = [
            {
              node.name = "source_filter.rnnoise"
            }
            {
              node.name = "sink_node.music-production"
            }
          ]
          actions = {
            create-stream = {
            }
          }
        }
      ]
    }
  }
]
