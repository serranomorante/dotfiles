pulse.properties = {
  # Keep desktop Pulse clients from pulling the PipeWire graph below the normal
  # workstation buffer. Low-latency production clients use JACK launcher-scoped
  # PIPEWIRE_* variables instead.
  pulse.min.req = 1024/48000
  pulse.min.quantum = 1024/48000
}
