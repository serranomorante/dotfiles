# Keep JACK defaults global. Per-app low-latency settings are injected by the
# REAPER, external Wine audio host, and AudioGridder launchers through PIPEWIRE_* variables.
#
# PipeWire's JACK config loader expects each drop-in to contain a valid
# SPA-JSON assignment; a comments-only file is reported as "Invalid argument".
jack.properties = {
}
